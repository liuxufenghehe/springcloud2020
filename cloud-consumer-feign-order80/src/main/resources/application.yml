server:
  port: 80

spring:
  application:
    name: consumer-feign-order
eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/,http://eureka7002.com:7002/eureka/

ribbon:
  ReadTimeout:  5000
  ConnectTimeout: 5000
logging:
  level:
    #设置feign监控指定服务指定日志级别
    com.jlxf.springcloud.service.PaymentFeignService: debug

